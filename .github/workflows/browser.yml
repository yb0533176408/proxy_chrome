name: Chrome GUI with Xpra

on:
  workflow_dispatch:

jobs:
  chrome:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      # Xpra 6 from the official repo + Hebrew fonts + file tools.
      # NOTE: we tunnel with localtunnel (loca.lt) instead of Cloudflare, because
      # the user's NetFree filter blocks *.trycloudflare.com (HTTP 418). loca.lt
      # is not blocked. localtunnel needs Node (preinstalled on the runner).
      - name: Install Xpra 6 + audio + Hebrew + file tools
        run: |
          sudo wget -q -O /usr/share/keyrings/xpra.asc https://xpra.org/xpra.asc
          sudo wget -q -O /etc/apt/sources.list.d/xpra.sources \
            https://raw.githubusercontent.com/Xpra-org/xpra/master/packaging/repos/noble/xpra.sources
          sudo apt-get update
          sudo apt-get install -y \
            xpra xpra-x11 xpra-html5 xpra-audio-server xpra-codecs \
            pulseaudio pulseaudio-utils dbus-x11 \
            gstreamer1.0-pulseaudio gstreamer1.0-plugins-base \
            gstreamer1.0-plugins-good gstreamer1.0-plugins-ugly \
            gstreamer1.0-tools \
            fonts-noto-core fonts-noto-color-emoji fonts-liberation culmus \
            x11-xkb-utils pcmanfm gpicview evince mousepad file-roller \
            unzip p7zip-full xdg-utils shared-mime-info desktop-file-utils locales
          sudo locale-gen he_IL.UTF-8 || true
          sudo update-mime-database /usr/share/mime 2>/dev/null || true
          sudo update-desktop-database 2>/dev/null || true

      - name: Install localtunnel
        run: |
          npm install -g localtunnel 2>&1 | tail -3 \
            || sudo npm install -g localtunnel 2>&1 | tail -3
          command -v lt && lt --version || true

      # Chrome: Hebrew UI + AUTOMATIC translate of non-Hebrew pages -> Hebrew.
      - name: Configure Chrome (Hebrew UI + auto-translate + downloads)
        run: |
          mkdir -p "$HOME/Downloads" "$HOME/.config/google-chrome/Default"
          printf '%s' '{"intl":{"accept_languages":"he,he-IL,en-US,en","selected_languages":"he,he-IL,en-US,en"},"translate":{"enabled":true},"translate_recent_target":"he","translate_allowlists":{"en":"he","fr":"he","ar":"he","ru":"he","es":"he","de":"he","it":"he"},"translate_blocked_languages":["he","iw"],"browser":{"has_seen_welcome_page":true},"download":{"default_directory":"/home/runner/Downloads","prompt_for_download":false,"directory_upgrade":true}}' \
            > "$HOME/.config/google-chrome/Default/Preferences"
          sudo mkdir -p /etc/opt/chrome/policies/managed
          printf '%s' '{"ForcedLanguages":["he","he-IL","en-US","en"],"TranslateEnabled":true}' \
            | sudo tee /etc/opt/chrome/policies/managed/hebrew.json >/dev/null
          xdg-mime default pcmanfm.desktop inode/directory 2>/dev/null || true
          xdg-mime default org.gnome.Evince.desktop application/pdf 2>/dev/null || true
          xdg-mime default gpicview.desktop image/png image/jpeg image/gif image/webp 2>/dev/null || true
          xdg-mime default mousepad.desktop text/plain 2>/dev/null || true

      # xpra default port 14500 is taken on the runner by a systemd socket -> we
      # bind 41234. --resize-display pins a sane screen. --keyboard-sync=no is
      # better over a high-latency tunnel.
      - name: Start Xpra
        run: |
          export LANG=he_IL.UTF-8
          export LANGUAGE=he:he_IL:en
          export LC_MESSAGES=he_IL.UTF-8
          mkdir -p "$HOME/Downloads"

          xpra start :100 \
            --bind-tcp=0.0.0.0:41234 \
            --html=on \
            --daemon=yes \
            --exit-with-children=yes \
            --resize-display=1280x720 \
            --pulseaudio=yes \
            --speaker=yes \
            --speaker-codec=mp3 \
            --microphone=no \
            --clipboard=yes \
            --clipboard-direction=both \
            --file-transfer=yes \
            --open-files=yes \
            --open-url=yes \
            --keyboard-sync=no \
            --webcam=no \
            --mdns=no \
            --notifications=yes \
            --bell=no \
            --quality=50 \
            --min-quality=20 \
            --start="pcmanfm /home/runner/Downloads" \
            --start-child="google-chrome \
              --lang=he \
              --no-sandbox \
              --disable-dev-shm-usage \
              --disable-session-crashed-bubble \
              --disable-sync \
              --disable-background-networking \
              --disable-background-timer-throttling \
              --disable-renderer-backgrounding \
              --disable-component-update \
              --disable-features=BackForwardCache \
              --no-first-run \
              --no-default-browser-check \
              --autoplay-policy=no-user-gesture-required \
              --renderer-process-limit=4 \
              --window-size=1280,720"

          for i in $(seq 1 30); do
            curl -s -o /dev/null http://localhost:41234 && break
            sleep 2
          done
          echo "Xpra is up."

      - name: Start localtunnel
        run: |
          # localtunnel: NetFree does NOT block loca.lt (unlike trycloudflare).
          nohup lt --port 41234 > tunnel.log 2>&1 &

          URL=""
          for i in $(seq 1 40); do
            URL=$(grep -o "https://[-a-z0-9]*\.loca\.lt" tunnel.log | head -1)
            [ -n "$URL" ] && break
            sleep 2
          done

          if [ -z "$URL" ]; then
            echo "localtunnel failed to start:"
            cat tunnel.log
            exit 1
          fi

          # The loca.lt reminder page asks the visitor to enter this IP once per
          # 7 days. We publish it so the launcher can show it to the user.
          IP=$(curl -s https://loca.lt/mytunnelpassword)

          FULL="${URL}/?sound=on&audio_codec=mp3&clipboard=on"

          echo "=============================================================="
          echo " OPEN THIS URL (Chrome recommended):"
          echo " ${FULL}"
          echo " If a 'Tunnel website ahead' page appears, enter IP: ${IP}"
          echo "=============================================================="

          # line 1 = URL, line 2 = IP=... (the launcher reads both)
          printf '%s\nIP=%s\n' "${FULL}" "${IP}" > "${GITHUB_WORKSPACE}/tunnel_url.txt"

      - name: Publish tunnel URL
        uses: actions/upload-artifact@v4
        with:
          name: tunnel-url
          path: tunnel_url.txt
          retention-days: 1

      - name: Keep Alive
        run: sleep 20400   # ~5h40m, stays under GitHub's 6-hour job limit
